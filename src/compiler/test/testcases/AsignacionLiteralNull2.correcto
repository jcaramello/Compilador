// Prueba de la correcta asignación de un literal null, que según la semántica debe ser de tipo C(Object) a un objeto de dicha clase.

class AsignacionTest{

	static void main()
	var Object o;
	{		
		o = null;
		
		if(o == null) {
			(System.printSln("Exito"));
		}
		else (System.printSln("Fallo"));
	}
}